-- Finalized production schema (v1)
-- No schema changes: all tables and columns are already created by previous migrations.
-- This migration marks the schema as finalized for client deployments.
-- When deploying to client: run `npx prisma migrate deploy` to apply any pending migrations.
SELECT 1;
